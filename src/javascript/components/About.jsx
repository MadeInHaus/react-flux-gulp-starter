'use strict';

var React = require('react');

var About = React.createClass({

    render: function() {
        return (
            <h1>This the about page.</h1>
        );
    }

});

module.exports = About;
